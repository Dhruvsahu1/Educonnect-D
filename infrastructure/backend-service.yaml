apiVersion: v1
kind: Service
metadata:
  name: backend
spec:
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 5000       # Service port
      targetPort: 5000 # Matches containerPort
  type: ClusterIP
